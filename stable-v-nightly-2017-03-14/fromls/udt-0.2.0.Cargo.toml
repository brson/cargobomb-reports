[dependencies]
bitflags = "0.7"
libc = "0.2"
log = "0.3"

[dependencies.libudt4-sys]
version = "0.2"

[package]
authors = ["Andrew Chin <achin@eminence32.net>"]
description = "Bindings to udt, a high performance data transfer protocol (based on UDP)\n"
homepage = "https://github.com/eminence/udt-rs"
keywords = ["udp", "socket", "udt", "udt4"]
license = "BSD-3-Clause"
name = "udt"
readme = "README.md"
repository = "https://github.com/eminence/udt-rs"
version = "0.2.0"

[target]
[target."cfg(windows)"]
[target."cfg(windows)".dependencies]
winapi = "0.2"
