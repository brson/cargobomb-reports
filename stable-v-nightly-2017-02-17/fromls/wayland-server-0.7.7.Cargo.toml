[badges]
[badges.travis-ci]
repository = "vberger/wayland-client-rs"

[build-dependencies]
[build-dependencies.wayland-scanner]
version = "0.7.7"

[dependencies]
bitflags = "0.7"
libc = "0.2"

[dependencies.wayland-sys]
features = ["server"]
version = "0.7.7"

[features]
dlopen = ["wayland-sys/dlopen"]

[package]
authors = ["Victor Berger <victor.berger@m4x.org>"]
build = "build.rs"
categories = ["gui", "api-bindings"]
description = "Bindings to the standard C implementation of the wayland protocol, server side."
documentation = "https://vberger.github.io/wayland-client-rs/wayland_server/"
keywords = ["wayland", "server", "compositor"]
license = "MIT"
name = "wayland-server"
repository = "https://github.com/vberger/wayland-client-rs"
version = "0.7.7"
