[dependencies]
[dependencies.rabbot]
version = "0.1"

[lib]
crate-type = ["dylib"]
plugin = true

[package]
authors = ["Will Crichton <wcrichto@stanford.edu>"]
description = "Compiler plugin for Rabbot"
keywords = ["abstract", "binding", "tree"]
license = "Apache-2.0/MIT"
name = "rabbot-plugin"
readme = "../README.md"
repository = "https://github.com/willcrichton/rabbot"
version = "0.1.0"
