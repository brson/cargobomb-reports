[dependencies]
[dependencies.sid]
version = "0.1.0"

[lib]
name = "sid_vec"
path = "src/lib.rs"

[package]
authors = ["Nicolas Silva <nical@fastmail.com>"]
description = "Vector and slice indexed with strongly typed handles instead of usize."
include = ["Cargo.toml", "src/*.rs"]
license = "MIT/Apache-2.0"
name = "sid_vec"
repository = "https://github.com/nical/vodk.rs"
version = "0.1.0"
